---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: redis
  title: Redis (IDP Cache)
  description: >
    Redis 7 instance used by the Nexus IDP portal for caching (catalog, auth sessions).
    Deployed in the backstage namespace as a single-replica Deployment.
  annotations:
    backstage.io/kubernetes-id: redis
    backstage.io/kubernetes-namespace: backstage
  tags:
    - redis
    - cache
    - platform
spec:
  type: service
  lifecycle: production
  owner: cloud-team
  system: internal-developer-platform
