apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${{ values.appName }}-backend
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: api.${{ values.appName }}.${{ values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${{ values.appName }}-backend
                port:
                  number: 80
