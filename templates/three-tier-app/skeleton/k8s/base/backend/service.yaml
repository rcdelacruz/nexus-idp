apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}-backend
spec:
  selector:
    app: ${{ values.appName }}-backend
  ports:
    - port: 80
      targetPort: ${{ values.backendPort }}
