apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: ${{ values.appName }}-db-pooler
spec:
  cluster:
    name: ${{ values.appName }}-db
  instances: 1
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
