# Retro: Backstage 1.52.1 → 1.53.0 bump (2026-07-23)

## Outcome
Clean bump. tsc passes across app + backend + 10 plugins. None of 1.53.0's 6 breaking
changes affected us. One real bump regression (Jest 30 jsdom) found and fixed.

## Fixed during the cycle
- Jest 30 unbundled jsdom → added `@jest/environment-jsdom-abstract` + `jsdom` at root.
- 4 pre-existing lint errors in `DatabasePicker.tsx` (surfaced by bump widening lint scope) —
  fixed with user approval (no-shadow, no-use-before-define ×2, no-nested-ternary).

## Backlog (deferred, not bump-blocking)
- **BUI token deprecation**: 4× `no-deprecated-bui-tokens` warnings in `Root.tsx`
  (`--bui-bg-neutral-1-hover/pressed`, `--bui-fg-primary`). Tokens still work in 0.17;
  migrate to the new semantic families (Accent/Warning/Negative/Positive) as its own task.
- **Repo-wide pre-existing lint debt**: `import React` no-restricted-syntax across many
  packages — MUST NOT fix (classic-JSX runtime; removal crashes the app).
- **No unit test coverage**: only test file is an e2e Playwright spec. Pre-existing known issue.

## Not done (gated)
- Deploy: base-image rebuild (`packages/backend/Dockerfile`) + `deploy.sh` to Talos homelab.
  Requires explicit go-ahead. AWS/ECS dormant, untouched.
- Commit: changes on `chore/backstage-1.53-bump`, uncommitted; parked `infra/` edits kept separate.
